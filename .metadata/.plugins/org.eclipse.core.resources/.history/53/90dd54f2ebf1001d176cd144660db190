package Integer;


public class LinkedList {
	Node head;
	public LinkedList()
	{
		head=null;
	}
	public void createLinkedList()
	{
		Node record=new Node(data);
		if(head==null)
		{
			head=record;
			
		}
		else
		{
			Node move=head;
			while(move.getNext()!=null)
				move=move.getNext();
			move.setNext(record);
			record.setPrev(move);
		}
	}
	

}
